plugins {
    id 'application'
    id 'idea'
}

mainClassName = 'com.epam.application.App'

dependencies {
    implementation project(':core')
}

jar {
    manifest {
        attributes("Main-Class": mainClassName)
    }
    from configurations.compileClasspath.collect { it.isDirectory() ? it : zipTree(it) }
}